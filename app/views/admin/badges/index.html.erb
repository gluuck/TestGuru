<div class="row mt-100%">
  <div class="col col-md-100%">
    <h1><%= t('.header') %></h1>
    <%= render 'shared/errors', resource: @badge%>
    <table class="table table-primary">
      <tr>
        <th class="sort-by-title pointer"><%= t('.title')%></th>
        <th><%= t('.picture')%></th>
        <th><%= t('.rule')%></th>
        <th colspan='3'><%= t('.action')%></th>
      </tr>
      <%= render @badges%>
    </table>
    <p><%= link_to t('.new_test'), new_admin_badge_path%></p>
    <p><%= link_to t('.new badge'), new_admin_badge_path%></p>
  </div>
</div>
