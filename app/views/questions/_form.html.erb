<%= form_with model: [@test, @question] do |form| %>
  <p>
    <%= form.label :body %><br>
    <%= form.text_field :body%>
  </p>
  <p><%= form.submit%></p>
<% end%>
<%= link_to 'Back', tests_path %>
